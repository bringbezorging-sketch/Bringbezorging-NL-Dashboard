export const metadata = { title: 'BringBezorging Dashboard' }
export default function RootLayout({ children }) {
  return <html lang="nl"><body style={{margin:0,padding:0}}>{children}</body></html>
}
